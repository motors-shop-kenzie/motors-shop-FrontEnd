/* import { iChildrenProps } from "@/interfaces";
import styles from "./styles.module.scss";

export const InputSectionField = ({ children }: iChildrenProps) => {
    return <section className={styles.inputSection}>{children}</section>;
};

 */
